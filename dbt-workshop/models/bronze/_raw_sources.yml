version: 2

sources:
  - name: raw
    description: External table over data in GCS
    schema: "{{ target.schema }}"
    tables:
      - name: instrument_revisions
        identifier: instrument_revisions_external
        description: >
          Some very useful comments
#        columns:
#          - name: instrumentId
#            description:
#          - name: instrumentName
#            description:
